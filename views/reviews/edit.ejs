<!-- New Review Form -->
<%- include("../partials/header")%>


<div class="container">
    <h1 class="text-center"> Edit your review for <%= adventure.name %> </h1> <!-- we could also do: <h1 style="text-align: center;" > same as BS3 class="text-center" -->
    <div class="row">
        <!-- don't use grid here: <div class="col-xs-3"> this does not work! -->
        <div style="width: 30%; margin: 30px auto;">    

            <!-- link to PUT request to the Update route, using method override  -->
            <!-- action: points to "/adventures/:id/reviews/:review_id", REST format -->
            <form action = "/adventures/<%= adventure._id %>/reviews/<%= review._id %>?_method=PUT" method ="POST">

                <div class="form-group">    <!-- adds margin below the input elt-->
                    <!-- we can't use value="" for textarea -->
                    <textarea class="form-control"  name="review[content]" cols="30" rows="10"><%= review.content %></textarea>
                </div>
                <div class="form-group">
                    <!-- btn-block ==> makes btn into block element -->
                    <button class="btn btn-lg btn-primary btn-block"> Submit </button>
                </div>
    
            </form>
            
            <!-- BS3 makes it so easy to have <a> tags as buttons -->
            <a class=" btn btn-default" href="/adventures"> See All Adventures </a>
        </div>
    </div>
</div>


<%- include("../partials/footer")%>
